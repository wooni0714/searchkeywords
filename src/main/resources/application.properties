spring.application.name=elk
server.port=8080

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false

# Redis
spring.data.redis.host=127.0.0.1
spring.data.redis.port=6380
suggest.cache.ttlSeconds=3
suggest.cache.minPrefixLen=2

# Elasticsearch
spring.elasticsearch.uris=http://elastic:LYkuKO9ZWro8mQ=FqPg-@localhost:9200
spring.data.elasticsearch.repositories.enabled=true

# JPA
spring.data.jpa.repositories.enabled=true
spring.datasource.url=jdbc:h2:mem:elkdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ??
logging.level.root=INFO

# ???
forbidden.pollInterval=5000

# ???
spring.profiles.active=local

elasticsearch.products.settings=products-settings.json
elasticsearch.products.mapping=products-mapping.json
